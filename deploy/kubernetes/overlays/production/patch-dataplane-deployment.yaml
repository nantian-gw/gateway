apiVersion: apps/v1
kind: Deployment
metadata:
  name: aether-gateway-dataplane
  namespace: aether-gateway
spec:
  template:
    spec:
      containers:
        - name: dataplane
          resources:
            requests:
              cpu: "2"
              memory: 512Mi
            limits:
              memory: 2Gi
          volumeMounts:
            - name: session-persistence
              mountPath: /etc/aether-gateway/session-persistence
              readOnly: true
      volumes:
        - name: xds-tls
          secret:
            secretName: aether-gateway-dataplane-xds-tls
            optional: false
        - name: admin-auth
          secret:
            secretName: aether-gateway-dataplane-admin-auth
            optional: false
        - name: session-persistence
          secret:
            secretName: aether-gateway-dataplane-session-persistence
            optional: false
