Below is a test case table that can be **directly copied to Excel / Jira / Feishu multi-dimensional table**.

## Recommended Header Columns
You can start with these 7 columns and add more later:
- `ID`
- `Priority`
- `Layer`
- `Test Case`
- `Preconditions`
- `Steps`
- `Expected Result`

Recommend adding 4 additional columns:
- `Owner`
- `Automated (Y/N)`
- `Execution Result`
- `Defect ID`

### Legend
- **P0**: Launch blocker, must pass
- **P1**: Strongly recommended to pass
- **P2**: Enhancement / execute per support scope
- **Layer**:
  - `IT`: Integration testing
  - `E2E`: End-to-end
  - `SEC`: Security
  - `PERF`: Performance
  - `CHAOS`: Fault injection / chaos
  - `REL`: Canary / release / rollback

> If certain capabilities are currently **not supported**, such as `GRPCRoute` / `TLSRoute` / `BackendTLSPolicy` / `Cache`, you can directly mark the corresponding rows as `N/A` without counting them as failures.

---

# 1) Basic Resources / Status / Lifecycle

| ID | Priority | Layer | Test Case | Preconditions | Steps | Expected Result |
|---|---|---|---|---|---|---|
| TC-001 | P0 | IT/E2E | GatewayClass is correctly taken over by this controller | CRD and controller installed; matching `controllerName` exists | Create a `GatewayClass` with matching `controllerName` and a `Gateway` bound to that class; wait for reconcile | `GatewayClass` `Accepted=True`; `Gateway` is taken over; generated Deployment/Service/LB resources are correct |
| TC-002 | P0 | IT | GatewayClasses not belonging to this controller are ignored | Other `controllerName` GatewayClasses exist in the cluster | Create `GatewayClass/Gateway` with non-matching `controllerName` | This implementation generates no resources, writes no unauthorized status, and does not incorrectly take over |
| TC-003 | P1 | IT | Legal `parametersRef` configuration takes effect | Parameter object available for reference | Create `GatewayClass` with legal `parametersRef`; observe rendered results | Parameters are correctly read; corresponding listener/dataplane config takes effect as expected |
| TC-004 | P0 | IT | Correctly reject on illegal/missing `parametersRef` | Construct parameter objects that are non-existent, wrong type, or have illegal content | Create several illegal `parametersRef` scenarios | `Accepted=False` or appropriate condition is correct; error reason/message is clear; no erroneous resources generated |
| TC-005 | P0 | IT/E2E | Gateway listener add/modify/delete takes effect | Valid `GatewayClass` exists | After creating `Gateway`, sequentially add listener, modify port/hostname/TLS, delete listener, and verify with concurrent requests | Configuration is eventually consistent; old listener is cleaned up; new listener is available; no residual routes |
| TC-006 | P0 | IT/E2E | Listener conflict / TLS Secret illegal handling is correct | Prepare port conflicts, protocol conflicts, and invalid certificate Secrets | Create conflicting listeners; then create an HTTPS listener referencing an invalid Secret | `Accepted/Programmed/ResolvedRefs` conditions are accurate; erroneous listeners do not take effect; other valid listeners are unaffected |
| TC-007 | P0 | IT | Status conditions and `observedGeneration` are correct | Normal Gateway is already created | Continuously modify spec multiple times; read status | `observedGeneration` follows the latest spec; `Accepted/Programmed/ResolvedRefs` status, reason, and message are accurate |
| TC-008 | P0 | IT | Deletion cleanup and finalizer are correct | Gateway/GatewayClass and generated resources already created | Delete `Gateway`, delete `GatewayClass`; observe finalizers and downstream resources | Finalizers are correctly removed; managed resources are reclaimed; no orphan resources; does not get stuck in terminating |

**Recommended tools**: `kubectl`, `kind`, `envtest`, `ginkgo/gomega`, `kuttl`

---

# 2) Route Binding / Cross-Namespace / ReferenceGrant

| ID | Priority | Layer | Test Case | Preconditions | Steps | Expected Result |
|---|---|---|---|---|---|---|
| TC-009 | P0 | E2E | Route in the same namespace successfully binds to Gateway | `Gateway` and `HTTPRoute` are in the same namespace | Create `Gateway` and `HTTPRoute.parentRefs`; send requests to verify | Route successfully attaches; traffic hits the corresponding listener/backend |
| TC-010 | P0 | IT/E2E | `allowedRoutes` `Same/All/Selector` behavior is correct | Prepare multiple namespaces and labels | Test `Same`, `All`, `Selector` separately; create cross-namespace Routes | Only allowed namespaces can bind; Routes not matching the policy do not attach |
| TC-011 | P0 | IT/E2E | Binding is rejected when `sectionName` / selector does not match | `Gateway` has multiple listeners | Route specifies incorrect `sectionName` or namespace label does not match | Route does not attach; status reflects rejection reason; no erroneous traffic enters |
| TC-012 | P0 | IT | `parentRefs` pointing to non-existent Gateway/listener is handled correctly | Non-existent Gateway or listener name | Create Route pointing to incorrect parent object | Does not attach; status is clear; controller does not panic or incorrectly write other object status |
| TC-013 | P0 | IT/E2E | Cross-namespace backend/cert is rejected without `ReferenceGrant` | Prepare cross-namespace Service/Secret references | Create Route/Gateway referencing backend or certificate in another namespace, but do not create `ReferenceGrant` | `ResolvedRefs=False` or appropriate status; traffic does not take effect; no unauthorized referencing |
| TC-014 | P0 | IT/E2E | Succeeds with `ReferenceGrant`; recalculates invalidation after revocation | Cross-namespace referenced objects already exist | Create `ReferenceGrant` and verify success; then delete `ReferenceGrant` | Reference is restored after grant creation; invalidates promptly after deletion; config recalculation is correct |

**Recommended tools**: `kubectl`, `kuttl`, `ginkgo`, custom echo backend

---

# 3) HTTPRoute Core Semantics

| ID | Priority | Layer | Test Case | Preconditions | Steps | Expected Result |
|---|---|---|---|---|---|---|
| TC-015 | P0 | E2E | Hostname exact match and wildcard match | Prepare multiple domains | Configure exact domain and `*.example.com`; send requests separately | Exact match takes priority; wildcard behavior meets expectations; non-matching domains do not hit |
| TC-016 | P0 | E2E | Path `Exact/Prefix` priority is correct | Prepare rules like `/a`, `/a/`, `/a/b` | Send multiple path requests | More specific rules take priority; consistent with Gateway API semantics |
| TC-017 | P1 | E2E | Header/query/method/multi-condition combination matching | Prepare multiple match combination rules | Use different header, query, method combinations to send requests | Only requests satisfying conditions hit; AND/OR logic matches implementation claims |
| TC-018 | P0 | IT/E2E | Multiple HTTPRoute merging and conflict ordering is correct | Multiple Routes point to the same listener/hostname | Create multiple routes with conflicting rules; send requests to verify | Conflict handling, priority, and merge results are stable; status is not chaotic |
| TC-019 | P0 | E2E | Backend weight 90/10 and `weight=0` behavior is correct | Both backend services are healthy | Configure 90/10 traffic split, load test and observe distribution; then set one path to `weight=0` | Traffic split ratio is close to configuration; `weight=0` backend receives no traffic |
| TC-020 | P0 | E2E | Backend Service/port does not exist, or no endpoints behavior is correct | Construct incorrect Service, incorrect port, and empty endpoints | Send requests separately | Status is correct; requests return expected error codes; no routing to incorrect targets |
| TC-021 | P1 | E2E | `RequestHeaderModifier` correctly modifies request headers | echo backend can echo request headers | Configure filters to add/delete/modify headers; send requests | Upstream sees request headers consistent with rules; no unintended modification of other headers |
| TC-022 | P1 | E2E | `ResponseHeaderModifier` correctly modifies response headers | echo backend can echo responses | Configure add/delete/modify response headers; send requests | Client receives response headers as expected |
| TC-023 | P0 | E2E | `RequestRedirect` takes effect, supports HTTP->HTTPS | Provide HTTP and HTTPS listeners | Configure 301/302/308, scheme/host/path redirect; send requests | Returns correct redirect code and `Location`; HTTP->HTTPS works normally |
| TC-024 | P0 | E2E | `URLRewrite` takes effect; rewrite and redirect conflict is rejected | backend can echo path/host | Configure path/host rewrite; then configure both rewrite+redirect simultaneously | After rewrite, upstream sees the changes; illegal combinations are rejected or status errors |
| TC-025 | P1 | E2E | `RequestMirror`, `timeouts`, `CORS` behavior is correct | Primary backend + mirror backend; browser or CORS testing tool | Configure mirror, timeout, CORS; verify separately | Mirror does not affect primary request results; timeout takes effect per config; preflight and response headers are correct |

**Recommended tools**: `curl`, `fortio`, `vegeta`, custom echo/httpbin backend, browser or `k6`

---

# 4) TLS / HTTPS / gRPC / WebSocket / TCP

| ID | Priority | Layer | Test Case | Preconditions | Steps | Expected Result |
|---|---|---|---|---|---|---|
| TC-026 | P0 | E2E | HTTPS terminate and SNI certificate selection is correct | Prepare multiple domain certificates | Configure multiple HTTPS listeners/certificates; access via different SNIs | Returns correct certificate; request enters correct Route |
| TC-027 | P0 | IT/E2E | Certificate hot reload, illegal Secret, expired certificate handling is correct | Rotatable certificate Secrets | Replace Secret content; test illegal cert/key, expired certificates | New certificate takes effect after hot reload; illegal certificate does not take effect and status is correct |
| TC-028 | P1 | E2E | `BackendTLSPolicy`: legal CA succeeds, SAN mismatch fails | HTTPS backend is prepared | Use valid CA/SAN; then create SAN mismatch | Legal scenario connects; illegal scenario handshake fails and validation is not bypassed |
| TC-029 | P0 | E2E | `GRPCRoute` Unary request routing is correct | Deploy gRPC echo service | Create `GRPCRoute`; call with `grpcurl`/`ghz` | service/method matching is correct; return code and metadata are normal |
| TC-030 | P0 | E2E | gRPC streaming, deadline, cancel propagation is correct | Provide streaming gRPC service | Initiate server/client/bidi streaming; test cancel/deadline | Stream is stable; cancellation and timeout propagate correctly; no abnormal stream disconnection |
| TC-031 | P1 | E2E | `appProtocol` `h2c/ws` selection is correct | Service configured with `appProtocol` | Test `kubernetes.io/h2c` and `kubernetes.io/ws` separately | Proxy protocol selection is correct; h2c/WS successfully upgraded or forwarded |
| TC-032 | P0 | E2E | WebSocket upgrade, long-lived connection, close behavior is correct | WebSocket echo service is deployed | Upgrade connection and continuously send/receive messages; test client close/backend close | Upgrade successful; messages complete; close behavior is predictable; long-lived connection is stable |
| TC-033 | P1 | E2E | `TLSRoute` / `TCPRoute` forwarding and long-lived connection stability | Execute only when supported | Configure passthrough / TCP forwarding; establish long-lived connections | SNI/TCP routing is correct; long-lived connection is stable; abnormal disconnection is recoverable |

**Recommended tools**: `openssl s_client`, `nghttp`, `h2load`, `grpcurl`, `ghz`, `websocat`

---

# 5) Rust Proxy Data Plane Features / Controller Behavior

| ID | Priority | Layer | Test Case | Preconditions | Steps | Expected Result |
|---|---|---|---|---|---|---|
| TC-034 | P0 | E2E/PERF | Upstream connection pool reuse and connection limit is correct | backend can observe connection count | Continuously send requests; observe keepalive reuse, connection count, and idle recycling | Connection reuse takes effect; connection count does not spiral out of control; pool limit and idle recycling match configuration |
| TC-035 | P0 | E2E | `fail_to_connect` / retry / failover / LB distribution is correct | At least 2 backends | Make one backend refuse connection/timeout; observe retry and switching; verify load balancing distribution | After failure, retry/switch according to policy; no infinite retries; load distribution matches weight |
| TC-036 | P0 | E2E | Large request body, slow upload, backpressure handling is correct | backend supports large body uploads | Upload large files; simulate slow upload using rate-limited/slow sending | No memory spikes; requests can be streamed; backpressure is normal; no abnormal timeouts |
| TC-037 | P1 | E2E/PERF | HTTP/2 concurrent streams, GOAWAY, RST handling is correct | HTTP/2 backend is deployed | Use `h2load` or client to create many concurrent streams; simulate GOAWAY/RST | Multiplexing is stable; connection close and stream reset behavior is correct |
| TC-038 | P0 | REL/E2E | Hot reload / rolling update / zero-downtime capability under long-lived connections | Continuous HTTP/WS/gRPC traffic exists | Update Route, certificate, and Deployment image under continuous traffic | Configuration switch is successful; disruption is within acceptable range; no widespread 502/503 |
| TC-039 | P0 | IT | Reconcile idempotency, rapid consecutive updates, leader switch is correct | controller supports leader election | High-frequency updates to Gateway/Route; restart leader; repeatedly apply the same config | Final state is consistent; no duplicate resources; recoverable after leader switch; no writing of out-of-scope status |
| TC-040 | P1 | IT/E2E | metrics / logging / status are consistent | Prometheus/logging system is integrated | Trigger success, failure, timeout, and binding rejection scenarios | Metrics, logs, and K8s status can be aligned with each other, facilitating troubleshooting |

**Recommended tools**: `fortio`, `wrk2`, `h2load`, `Prometheus/Grafana`, `kubectl logs`

---

# 6) Security Testing

| ID | Priority | Layer | Test Case | Preconditions | Steps | Expected Result |
|---|---|---|---|---|---|---|
| TC-041 | P0 | SEC | Request smuggling regression: `CL/TE`, `TE/CL`, illegal chunked | Can send raw HTTP messages | Construct multiple smuggling requests, covering cache-hit / keepalive / reuse scenarios | Gateway is not bypassed, requests are not mixed, cache is not polluted, no erroneous reuse |
| TC-042 | P0 | SEC | Duplicate headers, CRLF injection, oversized headers handled correctly | Custom raw request tool supported | Construct abnormal header messages | Illegal requests are rejected or safely handled; not injected downstream; resource usage is controlled |
| TC-043 | P0 | SEC | `Host` / `X-Forwarded-*` spoofing protection strategy is correct | Trusted proxy or direct connection scenario | Spoof Host/XFF/XFP separately; access via proxy and directly | Only trust legitimate proxy chain; no routing/auth errors due to forged headers |
| TC-044 | P0 | SEC/CHAOS | Slowloris / connection flood / idle timeout protection | Can initiate many slow connections | Establish many slow connections, half-open connections, and idle connections | Gateway can still serve normal traffic; connections and FD do not spiral out of control; rate limiting/timeout policies are triggered |
| TC-045 | P0 | SEC | Multi-tenant isolation, hostname hijack, log leak prevention, TLS validation cannot be bypassed | Multi-namespace, multi-domain scenario | Attempt unauthorized domain binding, cross-namespace referencing, trigger error logs, forge backend certificates | No unauthorized access, no token/secret leakage, no certificate validation bypass, log sanitization |

**Recommended tools**: `curl --raw`, custom Python/Go raw socket scripts, `openssl`, `oha`, security regression scripts

---

# 7) Performance / Stability / Chaos / Canary

| ID | Priority | Layer | Test Case | Preconditions | Steps | Expected Result |
|---|---|---|---|---|---|---|
| TC-046 | P0 | PERF | HTTP / HTTPS / gRPC baseline performance testing | Stable load testing environment and baseline backend | Test QPS, throughput, p50/p95/p99, TLS handshake, gRPC concurrency | Target capacity achieved; tail latency within SLA; no abnormal error rate |
| TC-047 | P0 | PERF | Configuration scale testing: large-scale Gateway/Route/listener/endpoints scenarios | Prepare generation script | Construct large-scale objects; test config propagation time, memory, CPU, reconcile latency | System can still converge after scale increase; resource usage is within acceptable range |
| TC-048 | P0 | PERF | 24h~72h soak: memory/FD/connection leak checking | Long-duration stable traffic | Run continuously for 24~72 hours, record CPU, RSS, FD, connection count | No obvious leaks; curves are stable; error rate does not continuously rise |
| TC-049 | P0 | CHAOS | Backend partial/complete fault injection | At least two backends | Inject timeout, RST, 5xx, full offline | Failure isolation is correct; retry/circuit-breaking/switch conforms to design; errors are observable |
| TC-050 | P0 | CHAOS | Gateway pod / node / controller / apiserver abnormal recovery | Can safely simulate failures | Kill pod, drain node, restart controller, disconnect watch | Business recovery within expected time; state converges; no widespread traffic interruption |
| TC-051 | P0 | REL | Canary 1%→10%→50%→100% and rollback verification | Prepare stable/canary dual GatewayClass or independent entry points | Increase traffic by ratio; observe metrics at each stage; trigger rollback drill | Error rate/latency stable during traffic increase; rollback completed within SLA; fast recovery after rollback |

**Recommended tools**: `fortio`, `vegeta`, `wrk2`, `ghz`, `Chaos Mesh`, `tc/netem`, `Prometheus/Grafana`

---

# 8) Optional Appendix: If You Enable Rust proxy Cache, Add This Set

| ID | Priority | Layer | Test Case | Preconditions | Steps | Expected Result |
|---|---|---|---|---|---|---|
| TC-C01 | P1 | E2E | cache hit/miss/stale/revalidate/purge is correct | Cache enabled; backend can control Cache-Control | Verify first miss, subsequent hit, revalidate after expiry, and proactive purge | Hit rate meets expectations; cache semantics are correct; purge takes effect |
| TC-C02 | P0 | SEC | Cache poisoning and cache-hit request smuggling regression | Cache is enabled | Construct abnormal headers, abnormal chunked, and variant key scenarios | Cache is not polluted; different requests do not interfere; hit path is secure |
| TC-C03 | P1 | PERF | Large object/chunked response cache consistency | backend returns large files/stream | Repeat download and compare digests; observe memory and disk/object storage usage | Content is consistent; resources are controllable; no truncation or out-of-order |

---

# 9) If Time Is Limited, Run This P0 Minimum Launch Checklist First

Recommend treating these as **blockers** first:

- `TC-001`
- `TC-002`
- `TC-004`
- `TC-005`
- `TC-006`
- `TC-007`
- `TC-008`
- `TC-009`
- `TC-010`
- `TC-012`
- `TC-013`
- `TC-014`
- `TC-015`
- `TC-016`
- `TC-018`
- `TC-019`
- `TC-020`
- `TC-023`
- `TC-024`
- `TC-026`
- `TC-027`
- `TC-029`
- `TC-030`
- `TC-032`
- `TC-034`
- `TC-035`
- `TC-036`
- `TC-038`
- `TC-039`
- `TC-041`
- `TC-043`
- `TC-044`
- `TC-045`
- `TC-046`
- `TC-047`
- `TC-048`
- `TC-049`
- `TC-050`
- `TC-051`

---

# 10) Recommend Adding an “Acceptance Criteria” Column in Excel

For example:
- Error rate `< 0.1%`
- p99 `< 50ms / 100ms / 200ms`
- Config propagation `< 5s`
- Rollback completion `< 5min`
- Memory growth `no continuous linear increase within 24h`
- FD usage `< 70%`
- Gateway status abnormal object count `= 0`

---

If you would like, I can continue directly with one of two implementation-ready versions:

1. **CSV format**, which you can directly paste into Excel
2. **Jira/Feishu multi-dimensional table field version**, including:
   - Module
   - Sub-module
   - Test Case Title
   - Preconditions
   - Steps
   - Expected
   - Priority
   - Automation Recommendation
   - Owner
   - Blocker Level

If you would like, I can also continue directly with:
**“Excel version split into four sheets: HTTPRoute / GRPCRoute / TLS / Canary Release.”**
